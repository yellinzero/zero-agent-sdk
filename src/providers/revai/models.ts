/**
 * Rev.ai transcription model catalog.
 *
 * @see https://docs.rev.ai
 */
export const REVAI_MODELS: Record<string, { description: string }> = {
  machine: { description: 'Machine — standard machine transcription' },
  low_cost: { description: 'Low Cost — budget-friendly transcription' },
  fusion: { description: 'Fusion — highest accuracy transcription' },
};
