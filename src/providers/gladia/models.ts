/**
 * Gladia transcription model catalog.
 *
 * Gladia uses a single default model for transcription.
 * @see https://docs.gladia.io
 */
export const GLADIA_MODELS: Record<string, { description: string }> = {
  default: { description: 'Default — Gladia pre-recorded transcription' },
};
