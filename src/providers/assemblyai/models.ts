/**
 * AssemblyAI transcription model catalog.
 *
 * @see https://www.assemblyai.com/docs
 */
export const ASSEMBLYAI_MODELS: Record<string, { description: string }> = {
  best: { description: 'Best — highest accuracy transcription model' },
  nano: { description: 'Nano — fast, cost-effective transcription model' },
};
